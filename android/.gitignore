.gradle/
.kotlin/
build/
local.properties
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Third-party Motion Photo samples for instrumentation tests — fetched on demand
# via fetch-test-assets.sh, not committed.
app/src/androidTest/assets/motionphoto/

# Release signing (keep these private; back them up!)
keystore.properties
keystore/
*.jks
*.keystore
